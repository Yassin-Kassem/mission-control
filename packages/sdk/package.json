{
  "name": "@mctl/sdk",
  "version": "0.1.0",
  "description": "Mission Control Drone SDK — build, test, and share custom AI drones",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "license": "MIT",
  "publishConfig": { "access": "public" },
  "keywords": ["sdk", "claude", "ai", "drones", "mission-control"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@mctl/core": "workspace:*",
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
